import tokens from "@vygruppen/spor-design-tokens";

export const zIndices = {
  hide: tokens.depth["z-index"].hide.value,
  auto: "auto",
  base: tokens.depth["z-index"].base.value,
  docked: tokens.depth["z-index"].docked.value,
  dropdown: tokens.depth["z-index"].dropdown.value,
  sticky: tokens.depth["z-index"].sticky.value,
  banner: tokens.depth["z-index"].banner.value,
  overlay: tokens.depth["z-index"].overlay.value,
  modal: tokens.depth["z-index"].modal.value,
  popover: tokens.depth["z-index"].popover.value,
  skipLink: tokens.depth["z-index"].skipLink.value,
  toast: tokens.depth["z-index"].toast.value,
  tooltip: tokens.depth["z-index"].tooltip.value,
};
