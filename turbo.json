{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "typegen": {
      "cache": true
    },

    "build": {
      "dependsOn": ["typegen", "^build"],
      "outputs": ["dist/**", "public/build/**"],
      "cache": true
    },
    "test": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["typegen", "build"],
      "cache": false,
      "persistent": true
    }
  }
}
